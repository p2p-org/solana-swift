name: Swift

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version'     
        required: true

jobs:
  upversion:
    runs-on: macos-12
    steps:
    - uses: actions/checkout@v3

    - name: upversion
      run: |
        ./upversion ${{ github.event.inputs.version }}
